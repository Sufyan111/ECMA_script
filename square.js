const findSquare = (num) => num.map((element)=>element**2)
    // {let square = [];

    // num.forEach ((element) =>square.push(element**2) );
    
// return square;}

console.log(findSquare([5,2,7,9,10]));